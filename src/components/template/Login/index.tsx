import { Card, Container, Input, Label } from "../../atom";
import Form from "../../atom/form";

export const LoginTemplate = {
  root: Container,
  form: Form,
  card: Card,
  input: Input,
  label: Label,
}
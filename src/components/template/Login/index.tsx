import { Button, Container, Input, Label } from "../../atom";
import Form from "../../atom/form";

export const Auth = {
  Login: {
    root: Container ,
    form: Form,
    input: Input,
    label: Label,
    button: Button,
  },
};
